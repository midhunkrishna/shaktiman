---
title: CLI Reference
sidebar_position: 1
---

# CLI Reference

`shaktiman` exposes every MCP tool as a CLI subcommand, plus `init`, `index`, `reindex`,
and `status`. `shaktimand` is the MCP stdio daemon.

:::note Placeholder

The full per-command reference — arguments, flags, output format, exit codes — is
authored in Step 5 directly from `cmd/shaktiman/*.go` and `cmd/shaktimand/*.go`.

:::
